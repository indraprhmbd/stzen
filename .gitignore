# Dependencies
node_modules/

# npm lockfile is committed: reproducible installs locally (`npm ci`) and in CI.
# (bun is not used; CI installs with npm ci.)

# Environment
.env
.env.local
.env.*.local
.dev.vars
server/.dev.vars

# Build output
client/dist/
*.tsbuildinfo

# Server compiled output (tsx runs from .ts, these are tsc remnants)
**/*.js
**/*.js.map
**/*.d.ts
**/*.d.ts.map
!client/src/vite-env.d.ts

# Restore migration files
!server/db/migrations/**/*.sql
!server/db/migrations/**/*.json

# Drizzle Kit
server/db/meta/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# Internal planning
_context/
.agents/
skills-lock.json

_temp/
.kilo/
.wrangler/

# Wrangler generated Env types are committed (config drift fails compile)
!server/worker-configuration.d.ts
